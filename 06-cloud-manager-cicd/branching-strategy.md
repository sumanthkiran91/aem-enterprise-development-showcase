# Branching Strategy

- `main` for stable release code.
- `develop` for integration.
- `feature/*` for active development.
- `release/*` for release hardening.
- Pull requests require code review and build validation.
